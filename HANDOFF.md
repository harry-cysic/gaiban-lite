# HANDOFF（2026-07-22，goal session 更新 · E6F 已放行 · Phase 1 已启动）

---

## 0. 最近一轮（Phase 1 启动）——先读这段

上一版 HANDOFF 写"本 session 未据 §10 重排优先级、未启动 Phase 1"。**已启动。**

**做了四件事**：

1. **补了 39.2 的 artifact**。TARGET/commit 里那个已放行的单路数字**在仓库里没有产物**
   ——两次跑都还躺在 titan064 的 `~/e0f-runtime/{e1f-released,e1f-postda4}`，从未拉回。
   已落盘到 `experiments/E6F-*/results/`。核对通过：39.292/39.231/39.226（离散 0.17%）、
   29.218/29.204/29.161（0.20%），且 `e1f-released` 的 argv **不带 flag** 而
   `attention_tp` 解析出 `tp_size=4`——默认已翻的两侧见证齐全。
   ⚠️ **教训**：跑完记得 rsync 回来并 commit，否则 headline 数字会变成无产物的孤儿。

2. **E7F 竖条开工**（`experiments/E7F-single-path-serving/`）。
   开工第一件事就修正了 §10 的一个隐含前提：**§10 把"最小单路 serving"写成
   HTTP + tokenizer + 请求循环，前提是裸引擎已能对真实 prompt 跑出图速度——它不能。**
   仓库里两条路径从未连过：`e0ef2e`（唯一真实 prompt 路径，`grep -c
   capture_stateful_graph` = **0**，全部质量证据在此）与 `e1f`（图速度 39.2，
   状态由 `build_seed_payload` **合成**）。

3. **交接已证逐位**（E7F 步 2，4 卡）：prefill → `seed_decode_payload` → decode，
   16/16 逐位、`max_abs` 0.0，分片默认（`tp_size=4`/16 头/`wo_b [4096,2048]` 见证在案），
   三种层型、整块链。**阴性对照做了**：扰动 `compressed` 一个元素 → 16 步全灭，
   所以这个门能失败。

4. **两条新记录进 TARGET**（§7.8 / §7.9），见下。

**发现里最要紧的一条**：**图 decode 路径只在 ctx ≥ 2047 存在**（§7.8）。
`(start_pos+1)//4 ≥ index_topk(512)`。约束本身 E1F 早写明，**但它的推论没人记过**：
- 已放行的 **39.2 是 ctx 2048 的数字**（M4 实测列已补上下文口径）；
- **ctx < 2047 的请求今天只能走 eager（210 ms/步）**——短 prompt 单路指标等于没有；
- §10 Phase 1 工作项表因此**补了两行前置**（状态交接、未饱和区快路径）。

**第二条**（§7.9）：**stateful 与非 stateful decode 不逐位**（4/16 位、≤1 ULP）。
归因干净：**捕图是精确的**（graph vs stateful eager 逐位 0.0，prefill 来的状态
与合成状态一样），差异全在两条 decode 实现之间。
⚠️ **机制未定位**；我先猜融合 HC，**猜错了**（`--hc-backend default` 返回 `None`，
两臂都没有融合链）。**别把这个猜测当已知重新捡起来。**
要紧之处：**D0L 的质量证据跑在非 stateful 路径上，serving 要跑 stateful 图路径。**

**试到 11 层（真实 stage 0 形状）失败**：门自己 OOM（23.18 GiB），因为它同时持有
prefill lane 状态 + decode 状态 + 两份快照。**不是 runtime 容量结论**，
且 8.6 GiB 的构成**未归因**。交接契约是逐层的，4 层已覆盖三种层型，故不影响结论。

⚠️ **操作教训（又一次）**：那个配置我跑了两遍，**第一遍是污染的**——开跑前
4 张卡各已占 15,375 MiB（上一轮残留）。我打了 `nvidia-smi` 快照**却没有据此中止**，
于是照跑并得到一个假的 OOM。**光打快照不够，要让脚本 precheck 不干净就退出**；
现在的命令里是 `if [ total -gt 16 ]; then exit 9; fi`。
另：本轮多次出现"跑完进程仍驻留、GPU 15 GiB 不释放"，
每次都要 `pkill -f "e7f_handoff_gat[e]"`（方括号防自匹配）再确认 1 MiB。

**下一步（E7F 步 3，未开始）**：16 卡、真实 D0L prompt（**须 ≥2047 才走得上图路径**）、
prefill → 交接 → 捕图 → decode，判据是生成 token 与冻结 golden 一致。
建议做法：**给 `e0ef2e_golden_gate.py` 加一个默认关闭的图 decode 臂**，
而不是新写一个 16 卡管线——它已经有 PP4 拓扑、真实 prompt、embed/head、golden 比较，
默认关闭则所有冻结臂逐字节不变（E4F 的 `indexer_qat_mode` 是同样的做法）。

---

面向继任者。**权威目标定义是 [`docs/TARGET-v4-flash.md`](docs/TARGET-v4-flash.md)**，
本文不重复它已写的东西（实测值、已证伪假设、硬规则、环境坑）。本文只写三件它没有的：
**手上未完成的事**、**我原本打算的下一步**、**尚未写进任何 README 的隐性知识**。

---

## 1. 未完成的事

### 1.1 交付状态

本轮 goal session 完成 E2F / E3F / E4F / E5F / E6F、D0L golden 扩展与独立重抽、
以及 §7.7 行 1 的 8 卡容量复测。全部已落盘 commit。

改了 TARGET 的结论：335 tok/s 单用户天花板被证伪（真值 76.2，因 DP-attention
复制 attention 权重）；8 卡 TP4×PP2 当前形态装不下 43 层；两个逐位融合核设为默认；
**E6F 的 −3/512 判为抽样噪声**（但 §1.3 字面仍 FAIL）；
**`max_seq_len` 载入期零成本、真正的显存驱动是 MoE prefill 缓冲**（见 §3.8）。

⚠️ **2026-07-22：TARGET §1.2 折扣改为按负载分列**（decode 30–40% / prefill 20% /
单路 20%），反推值随之下调（单路 187.5、专业版 prefill 37,500、标准版 prefill 18,750），
**原先"计划值低于反推"的三行现在都被覆盖**。
⚠️ **TARGET 新增 §10 交付路线**（Phase 1 专业版单路 serving 做实 → Phase 2 专业版其余行
→ Phase 3 标准版容量与 CPU offloading）。**§10 是 §7 的上位约束**：§7 排"下一个实验"，
§10 排"下一个交付"。~~本 session 未据 §10 重排优先级、未启动 Phase 1。~~
**Phase 1 已于 2026-07-22 启动，见本文 §0。**

### 1.3 本轮竖条一览（E2F → 8 卡容量复测）

| 竖条 | 结果 | 状态 |
|---|---|---|
| **E2F** | B=1 decode profile；证伪 335 tok/s 天花板（真值 76.2） | 已落盘 |
| **E3F** | 8 卡当前形态装不下；M7 证伪 | 已落盘；纸面账**已被实测证伪**（见下） |
| **E4F** | 融合 indexer QAT 核，逐位，+2.31% | **已设默认** |
| **E5F** | 融合 KV-latent FP8 QAT 核，逐位，+2.30% | **已设默认** |
| **E6F** | attention TP4 分片（A 档）：decode **+34.5%**、prefill **+13.6%**、每卡省 1.547 GiB | **已放行、已设默认**（门基准迁移后两条判据 PASS） |
| **D0L 扩展 + indep** | golden 上限解除；v2 640 位、indep 927 位（独立重抽） | 已落盘 |
| **§7.7 行 1** | 分片后 8 卡单 stage 上限 **21 层**（PP2 需 22），缺 ~0.40 GiB/卡 | 已落盘 |

**E6F 已放行（2026-07-22）**：人决定把 §1.3 门基准迁到 D0L v2（640 位），
两常数由该集未分片基线重新标定，重判 **两条判据都 PASS**
（分数 614/640 打平；`top2_gap` 0.955883 ≤ 包络 0.959503，**包络常数逐位未变**）。
基线确定性已复跑验证。**默认已翻**（`attention_tp_shard=True`，
`--no-attention-tp-shard` 可退）。
⚠️ **口径断层**：**此前所有"不带 flag"的数字都是未分片的**（E1F 29.2 tok/s 即是），
翻默认后同一命令给出分片结果，**不可直接比较**。
⚠️ **必须同时看见**：E6F 本身没变——v1 子集上仍是 **491 vs 494**，是**门迁移**让它过的。

**E6F 的质量结论（迁移的依据）**：两组独立 golden 一致判定
冻结集上的 **−3/512 是抽样噪声，不是系统性回归**——
indep 927 位净 −1（7 差/6 好，p=1.000）、v2 640 位 **614/614 打平**（3 差/3 好，p=1.000）。
决定性细节：640 位那组里前 8 条**逐字复现 494/491**，而新增的 8192 档
**120→123 把 3 位还了回来**。门是确定性的，所以这是**抽样**噪声不是**测量**噪声。
~~**但 §1.3 按字面仍 FAIL（491 < 494）**，放行 = 重新标定门基准 = **人的决定**，
本 session 未作此决定，E6F 维持默认关闭、数字不进实测列。~~
⚠️ **上面这段是放行前的记述，已被本节开头那段取代**（人已于 2026-07-22 决定迁移
门基准，重判两条判据都 PASS，E6F 已放行、已设默认、数字已进实测列）。
保留原文是因为"491 < 494 在旧集上是事实"这一点仍然成立，**不保留则会显得
E6F 是靠自身变好过的门**——它不是。

⚠️ **E4F/E5F 的 +2.31%/+2.30% 是 `da4cd69` 之前测的**（该 commit 从
`fp4_quant_dequant` 移除了 8 个全尺寸常量分配，逐位安全但在 decode 路径上）。
改后重测闭环 **29.2 tok/s**（vs 改前记录的 28.992）——差值 +0.7% **低于串行 A/B
的分辨率（§9.1 说 1–4% 不可靠）**，故记为"无回归"，**不归因于 da4cd69**。
要归因需与改前代码做成对交替 A/B。

### 1.4 E4F 的历史状态（已完成，保留供追溯）

- `runtime/dsv4_direct/ratio4_attention.py` 已加 `indexer_qat_mode`（默认 `ref`，
  env `DSV4_INDEXER_QAT_DECODE` 可覆盖），两个 decode 调用点走 `_indexer_qat()`。
  **默认关闭，未改变任何既有语义。**
- `runtime/e2f_decode_phase_probe.py` 已加 `--mode ab`：两条常驻 lane 背靠背
  交替重放（TARGET §9.1 口径），顺带每步逐位对拍作为层内数值门。
- E4F 微基准已完成并落盘：decode 形状下融合核**逐位相等**（max_abs_diff 0.0），
  省 **55.2 µs/次调用**，轮间离散 <1%。
- **未完成**：层内 A/B 尚未跑出数字。跑通前不得引用任何层内收益。

### 1.2 挂着的账（按我认为的紧要程度）

1. **C3F↔C2F 矛盾读数的成因未坐实**。chunked prefill 的"快 1.5–2.3×"已被 C2F 同口径
   证伪（证伪是实测），但**为何 C3F 会读到相反结果，目前仍是推断**（疑为整段臂在
   0.6 GiB 余量下的分配器压力）。补一个对照臂即可了结：C3F 的 E2E 口径 + 整段 +
   只注册 1 种形状 + 充足 free，一次运行。
   ⚠️ 本仓库已被同一形状的"分配器假说"坑过一次（第二十二竖条花整条竖条证伪它，
   真因是 NCCL 静默回退 SHM）。**在补测前不要把这个假说当已知**。
2. **chunked prefill 没有自有 golden**，因此只能作为 Phase 4 能力存在，不能默认开启。
   现状：语义已证精确（状态机逐位 27/27、索引集精确 18/18），但 chunk=512/999 在
   D0L 上越出 `top2_gap` 包络。需要一个"分段 vs 整段"的专用 golden 才能放行默认开。
3. **两个已否决杠杆的复活条件已写明但未做**：prefill HC 融合（+19.9%，卡在 vLLM
   kernel ≥1024 行数值错误，分派点见 TARGET §7.4）；tilelang 稀疏核包装层 16.1%
   搬运税（去掉不需要新 kernel，只需出口按 head chunk 分块不拼回）。
4. **`run_e0mf_titan.sh` 故意不带 `NCCL_P2P_LEVEL=SYS`**——它的冻结产物就是在无 P2P
   下产生的，加上会与冻结值对不上。若要统一，必须同时重生成其冻结产物。

---

## 2. 我原本打算的下一步

⚠️ **2026-07-22：TARGET 新增 §10 交付路线（三阶段），它是 §7 的上位约束。**
本节写的是"实验层面的下一步"；**交付层面以 §10 为准**。
**Phase 1 已启动**（E7F，见 §0）；下面这张实验层面的排序仍然有效，
但它排在 §10 之下。

按我当时的判断，实验层面的顺序是：

1. **FP8 常驻 attention 权重**——现在是**唯一对得上 8 卡缺口的杠杆**
   （缺 ~0.40 GiB/卡，估 +0.91）。未实现、无质量门。取舍依据见 §3.6。
2. **尾巴折叠继续按链拆**——下两个目标 `index_topk_done`（44.5 µs/层）与
   `raw_kv_done`（30.1）。状态见 §3.7。
3. ~~E6F 放行与否~~ → **已了结（2026-07-22 放行、设默认）**。
   遗留：**翻默认后的口径重测**——分片已成默认，故所有"当前数字"需在新默认下重测。
   **单路已重测：29.2 → 39.2 tok/s**（+34.2%，p50 25.49 ms，bitwise 132/132，
   16 卡口径）——已写入 §2 模式矩阵 M4 实测列。
   **聚合与 prefill 仍未在新默认下重测**，其现存数字都是**未分片口径**。
4. **§7.7 行 2–5**：模型装得下只回答了行 1 的前置；各行的 KV 池预算都还没算。

## 3. 隐性知识（没写进任何 README，但会决定结论真假）

### 3.0 本轮新增的方法学（E2F/E3F/E4F）

1. **图内相位计时要 `external=True`**。`torch.cuda.Event(enable_timing=True)`
   在 capture 期间能被录成 event-record 节点，但之后 `elapsed_time` 返回
   `cudaErrorInvalidValue`；`torch.cuda.Event(..., external=True)` 才可查询。
   已封装为 `dsv4_direct/phase_timer.py:GraphPhaseRecorder`。
   **标记不是免费的**：每个是图里一个真实节点，244 标记 = +14.8%，47 标记 = +2.0%
   （约 3.3–4.6 µs/标记）。**相位表只做定性分相，定量归因用 nsys。**
2. **nsys 必须带 `--cuda-graph-trace=node`**，否则一次 graph replay 在报告里是
   一个不透明 range。配合探针的 `--cuda-profiler-range`（cudaProfilerStart/Stop）
   把 load/warmup/capture 排除在报告外。
3. **被 trace 的 rank 会把自己的减速通过集合通信传给同组其他 rank**：4 个 rank
   全读到 8.23 ms（未 trace 7.62）。nsys 运行的**绝对 ms 不可用，份额可用**。
4. **不用标记也能分段**：一次 replay 内 kernel 序列固定，MoE 集合通信是天然分隔符
   （`ReduceScatter` 每层一次；层内第一个 `AllGather` 分 attention/MoE）。
   见 `experiments/E2F-*/analyze_regions.py`——**先对层数校验计数再报数**，
   否则 MoE 结构一变就会静默错分。
5. **MoE 对象在 lane 之间是共享的**（`physical_stage` 只给每条 lane 自己的
   state/attention）。所以**两条 graph lane 不能都用 slots 1-3**——第二次 capture
   会以 `slot is not clean` 失败。做双 lane A/B 必须给变体 lane 独立 slot 区间
   （探针用 5-7，`slots_per_shape=8`），**且变体 lane 的 warmup 也必须用它自己的
   slot**，否则会把基线 lane 的 slot 弄脏。这一条咬了两次。
6. **OOM 信息里的"另一个进程"可能是上一次跑挂的残留**。E2F 曾把一次残留导致的
   OOM 误读为自己的内存回归；`nvidia-smi --query-compute-apps` 先看一眼再改代码。
   清理用 `pkill -f "<pattern 带方括号>"`（§3.3.2）。
7. **env 开关经 ssh 传远端时会静默失效**。`run_*.sh` 里 `ENV_BASE` 是**单引号**
   字符串，往里插 `${VAR:-:}` 不会在本地展开——它作为字面量传到远端，而远端没有
   该变量，于是展开成 `:`（空操作）。**症状是"杠杆一点效果都没有"，与"杠杆无效"
   完全同形**。E4F 因此差点把一个实测 −3.82% 的融合读成 0%。两条要求：
   - 插入点必须让**本地**展开：`ENV_BASE='...'"; ${VAR:-:}"`；
   - **结果 JSON 里要有处理组见证**。已给 `e1f_full_decode_bench.py` 加
     `attention_modes`（记录每层实际解析出的 `indexer_qat_mode` 等），
     没生效的开关在 artifact 里看得见，而不是被读成阴性结果。
   这与 §3.0.6、TARGET §9.10 是同一类教训：**静默失效的机制比崩溃危险**。
7b. **第四次"静默失效"：`run_*.sh` 的位置参数不透传**。
   `run_e1f_dual.sh` 只取 5 个位置参数，多给的 `--attention-tp-shard`
   被直接丢掉；而分片状态在 `tp_size`/`tp_rank`，不符合 `*_mode` 命名，
   自动发现见证也没记。结果是"分片在 16 卡上没有任何效果"——与真的无效同形。
   已加 `"$@"` 透传，并把 **`sys.argv` + 解析后的 `attention_tp`** 一起写进
   E1F 结果。**规则升级见 TARGET §9.11：见证要记"要求侧"与"解析侧"两边。**
8. **headline tok/s 只按部署档位整系统计**（TARGET §9.13，2026-07-21 定）。
   专业版 = 一套 16 卡系统，标准版 = 一套 8 卡系统；**不得跨档位折算**，
   `tok/s/GPU` 不作 headline。容量按卡、诊断按 stage/层 仍合法，标注清楚即可。
   历史文档里的跨档位折算已**就地标注为"无效折算"而非删除**
   （C1F 的 785 tok/s/GPU、C2F 的 713 in-tok/s/卡、立项报告的 2×单机 10–16K）。
   ⚠️ 最容易犯的一处：**"双机系统里的一台机器"不是"一套单机系统"**——
   前者每机 2 个 stage / 22 层，后者要独立扛 43 层，E3F 已证后者只在最激进
   分片下才勉强装下。
9. **`runtime/out-*/` 里有被 Git 跟踪的冻结产物，重跑同名配置会直接覆盖它们。**
   E5F 跑 prefill 回归时用 `run_c2f_prefill_titan.sh 8192 w4a16 fused`，
   输出文件名只由 `chunk/moe/indexer` 决定，于是**两次运行写同一个文件、
   并覆盖了 C2F 冻结的 `out-c2f/c2f-chunk8192-w4a16-*.json`**（已 `git restore`
   还原）。更坏的是：两个臂互相覆盖，最后两份"结果"字节相同，
   差点被读成"处理无效"。
   已给该脚本加 `C2F_OUT_DIR` 覆盖，**跑对照实验时务必给每个臂独立 out-dir**；
   跑任何 `run_*.sh` 前先 `git status runtime/out-*` 看一眼会不会覆盖冻结产物。
10. `runtime/dsv4_direct/__pycache__/*.pyc` **是被 Git 跟踪的**（历史遗留，21 个）。
   改 `dsv4_direct` 下的文件后 `git add -A` 会带进 .pyc 噪声；本轮 commit 都是
   显式加路径避开的。

### 3.1 测量方法学

1. **4090 上串行 A/B 计时不可信**。同配置三轮可给出 51/219/318 µs 三个完全不同的
   差值（时钟/热漂移）。所有 graph-replay 级 A/B 必须用**成对交替重放**：两条 lane
   常驻、每步背靠背、逐步交替顺序。MTP 与 HC 的可信数字全出自这个口径。
2. **per-component `synchronize` 计时的开销随 forward 次数增长**（1024 段长的臂上
   +14.7%）。`component_walls` **不能跨 forward 次数不同的臂相减**；headline 一律用
   未插桩 p50。C4F 的分相探针是反例做法：插桩开销 0.13%、相位覆盖 100.02%，因为它
   用 CUDA event 而非 host sync。
3. **一个组件的桶会吸收整条链的跨 rank 偏斜**。C2F 的 MoE 桶曾被记成 131 ms/层，
   隔离测只有 49.8——因为 MoE 是链上唯一含集合通信处。**看到某个桶异常大时，先问
   "它是不是链上唯一的同步点"，再怀疑该组件本身。**
4. **微基准 ≠ 层内**。C4F 的 cast 提取微基准预期 0.65 ms/层，层内只兑现 0.166 ms
   （已被分配器复用/与 GEMM 重叠）。反向也成立：A5F 的 HC 微基准 2.92× 在集成态
   只回收 4.7 ms/stage。**微基准只用于判活/判死，不用于折算收益。**
5. **≥3 轮 + 报轮间离散**。已放行数字的离散都在 0.03–0.29%；超过 1% 说明口径里有
   未控变量（经验上：NCCL 传输选择、分配器状态、或插桩本身）。

### 3.2 质量门的实际判据

1. **`top2_gap` 包络比分数更硬**。D0L 基线最大近平局间隙 **0.9595**；分数 ±3 在
   "换一组 prompt 就可能翻号"的范围内，而**越出包络是质量性变化**。prefill HC 融合
   正是因越包络（1.4929）被否；chunked prefill 的 chunk=512/999 同理。
   **放行判据 = 分数不降 且 gap 不越包络。**
2. **Marlin 分组 GEMM 对 batch 组成是 1–2 ULP 敏感的**（行分组变了求和序就变）。
   这是固有性质不是 bug。凡改变行集合/行序的改动——失步投机、集合重叠的行分块、
   chunked prefill——**都不可能逐位，别浪费时间追**。
3. **跨 TP lane 的输出本来就不逐位**（reduce_scatter 求和序），规范输出取 tp_rank0。
   种子残留态下各 lane 的 argmax 会分叉、真实 prompt 下则一致——**用种子态做
   "lane 一致性"检查会得到假失败**。
4. **exact-topk 门对 E2M1 格点翻转敏感**：两条独立公式在 1 ulp 差处会翻转一个量化码，
   进而换掉一个近平局 top-k 条目。E0ff 的种子 **20260717** 是筛出来的"全部抽样落在
   格点上"的种子，**不要随手改**。
5. **HC 类数值门只在真实权重上有意义**：真实 `hc_scale` 是 0.03–0.20，会压制 GEMM
   误差向 post/comb 的传播；合成 `hc_scale=1` 放大约 4×。A5F 的"≤1e-5"就是这么来的假象。

### 3.3 集群与工具

1. **16 rank 跑挂后集合通信会楔死**，需在两台机 `pkill -f <bench>`；TORCH_NCCL 超时
   是 120 分钟，等不起。
2. **`pgrep` 自匹配**：ssh 起的 `bash -c` 里若内嵌 pgrep 的模式串，pgrep 会匹配到
   自己的父 shell，给出假的"有残留进程"。独立执行才准。
3. **Bash 工具里的 `cd` 跨调用保持**，曾导致 `git add` 路径落空。用绝对路径。
4. **长 commit message 里的引号会破坏 shell 引用**，用 `git commit -F <file>`。
5. **单卡微基准优先放 titan065**，避免与 titan064 上的 16 rank 作业抢卡。
6. 热 page cache 下 11 层 stage 加载约 11–12 s；首次加载慢很多。
7. `runtime/` 不是安装包：一律 rsync 到 `titan064:~/e0f-runtime/`（部分微基准在
   `titan065:~/a5f/`），靠 PYTHONPATH 串起来。

### 3.4 子 agent 协作（若继续用多 agent）

1. **agent 会在"等监视器"处提前停下**而不是跑完（本 session 发生 2 次）。症状是报告
   里写 "waiting for X to finish"。解法：SendMessage 明确要求**同步执行到完成，
   不要用等待/监视器方式停下**。
2. **完成后仍会持续收到滞留的监视器通知**，内容是旧结论的复述。不要据此重做工作。
3. **数字必须从拉回本地的 per-rank JSON 复核**。本 session 出现过与实际不符的完成
   通知。另：`e0ef2e` 的 `result.json` 是人工挑选的字段子集，新加字段会被静默丢弃，
   分析须以 per-rank JSON 为源。
4. **归因、复测、跨实验对账必须在主会话做**。子 agent 只看得到自己那一片，本 session
   有两次是主会话对账才发现问题：C2F 的 131 ms 归因错误、以及我自己把"推断"写成
   "实测"（由另一个 agent 复核时抓出来）。

### 3.5 门脚本命名对照

| 脚本 | 覆盖 |
|---|---|
| `e0ef` / `e0ff` / `e0wf` | ratio-128 / ratio-4 / 窗口层 attention oracle |
| `e0cf` / `e0df` / `e0sf` | TP4 MoE / 整层 block / superstage + stateful graph |
| `e0pf` / `e0qf` | 单机 TP4×PP2 / 双机跨机管线 |
| `e0ef2e` | E2E golden（短门；`--prefill-chunk` 为 D0L 长门臂） |
| `e0kf` / `e0mf` | fp8-KV ratio-4 配对门 / MTP block oracle |
| `e1f` / `e1if` | 满配吞吐 B 扫描 / 4-microbatch 交织 |
| `c2f_*` / `c4f_*` | prefill stage bench 与各类探针、分相 profile |

---

### 3.6 E6F 三个档位的取舍依据（**从 commit 重建不出来**）

竖条只落地了 A 档。为什么不是 C、以及"下一个该做哪个"，靠的是一次**交叉**
计算（`docs/design-attention-tp4-sharding.md` §6），8 卡 PP2 一个 stage：

| 方案 | attention/卡 | 合计 | 余量 | 新增集合通信 |
|---|---:|---:|---:|---:|
| 现状（DP，BF16 常驻） | 5.09 GiB | 26.84 | −3.32 | +0 |
| **A（只切 o-path）** | 2.00 | 23.75 | −0.23 | **+1/层** |
| C（o+idx+column-parallel） | 1.28 | 23.03 | +0.49 | **+5/层** |
| **A + FP8 常驻** | **0.86** | **22.61** | **+0.91** | **+1/层** |
| C + FP8 常驻 | 0.60 | 22.35 | +1.17 | +5/层 |

**关键判断：`A + FP8 常驻`(+0.91) 的余量比 `C`(+0.49) 更大，而通信是 1/层 vs 5/层。**
所以**容量的更优杠杆不是"继续分片"，是"别在 load 时把 FP8 升成 BF16"**——
checkpoint 里非专家权重**本来就是 FP8**，是 runtime 载入时升上去的（E2F）。
它与分片**正交、不花通信**。故顺序是 **A → 评估 FP8 常驻 → （很可能不需要）C**。

先做 A 的另一半理由是**几何干净**：`o_groups=8` 与 TP4 整除、每 rank 恰好 2 组，
**不新增集合通信种类**，且吃到 2.49× 字节收益的大头。

⚠️ 两条限制，别丢：
1. FP8 常驻是**语义变更**（计算路径改变），要**独立质量门**；分片只是求和序重排。
   但它更像"别再上升"而不是"新加量化"。
2. **上表余量是 B=1 / max_seq 3328 口径。装得下模型 ≠ 交付得了标准版五行**，
   128K 与 50 路并发各自需要真实 KV 预算——那是 §7.7 逐行要算的，
   **不要用本表余量替代**。
3. ⚠️ **本表是纸面账，而纸面账在本栈上已连续两次偏乐观**：它预测 C 有 +0.49 余量、
   E3F §4 预测分片后 22 层可放（余 0.50），**实测 22 层 OOM、上限 21 层**。
   所以表里的相对排序可信（同一套假设下横向比较），**绝对余量必须实测复核**。

### 3.7 尾巴折叠：按链拆到了哪一步（**接续点**）

总量（E2F）：一次 stage replay **2,584 个未融合 eager 逐元素核**、71 种核型、
平均 1.18 µs、**无单一主犯**，共 3.06 ms/replay，四级流水下 **12.1 ms/token**，
占 replay **39.5%**。**每步固定**（B=1→B=8 核数 1,980→1,983，时间仅 +17%）。

**收费机制（决定一切排序）**：**按核数收费，不按字节**——平均 1.03–1.73 µs，
都贴着 4090 的最小 kernel 时长。所以**减少核数≈换成时间**，
但换算效率约 **73–89%，不是 1:1**（融合核自身也要时间）。

区域：**attention 体内 77%**（213.8 µs/层、180.6 核/层），MoE 区 23%。

**已拆掉的两条链**（都逐位、都已设默认）：

| 链 | 竖条 | 层内 A/B | 闭环 | 备注 |
|---|---|---|---|---|
| indexer QAT | E4F | −3.82%/stage | 27.740 → 28.381 | 只在 ratio-4 层跑 |
| KV-latent FP8 QAT | E5F | −2.25%/stage | 28.402 → 29.055 | **每种层型都跑** |

**下两个目标（E2F §5b 排序）**：`index_topk_done`（**44.5 µs/层**）、
`raw_kv_done`（**30.1 µs/层**，其中 `rms_norm`/rope 那一半）。
一个 stage 的非 GEMV 约 **1.65 ms**（ratio-4 层 234 µs/层、ratio-128 122 µs/层），
**E4F 已取 0.29 ms**。

⚠️ **该排序表是"排序用估算"，引用前必须层内 A/B 复核。**
⚠️ **decode 与 prefill 的链排序不可互换**：同一段 KV FP8 QAT 代码在 decode 是
~15 µs/层的 launch-floor 工作，在 prefill 只占 0.14%（C4F 冻结分相）。
**launch-floor 绑定是 decode 现象**；C4F 的 90.5× **不可折算到 decode**。

### 3.8 本轮最贵的教训：显存归因连错三次

同一个量（"8320 下什么在吃显存"）我归因了三次：**可忽略 → 主导 → 都不是**，
**三次都是先断言后验证**。真相是 **MoE prefill 缓冲按 prompt 长度派生的
row shape 逐个预分配**（`e0ef2e_golden_gate.py:938` `global_row_shapes`），
8192 那条单独约 3.06 GiB；`max_seq_len` 在载入期**零成本**（4224 与 8320
实测 stage 0 均 4.23 GiB free）；KV 被压缩比 4/128 压到全 stage ~15 MB。

**可复用的判据**：
- **反向读数=污染**：那次 0.21 GiB 用了 `--embed-device cpu`，本应让 free**变大**
  却**变小**了。**读数与干预方向相反时，先怀疑环境而不是记录结论。**
  起因是我 kill 掉死锁后**没验证显存已释放**就立刻起下一跑。
  **每次开跑前 `nvidia-smi` 确认 1 MiB**——本轮后半段全部这么做。
- **逐字节相同的两次失败**既像"杠杆用错地方"也像**静默 no-op**（本竖条已犯四次）。
  判别法：**换一个能跑完的配置再比**（22 层两次 OOM 数字全同 → 改测 18 层，
  bf16 3.95 vs fp8 3.96，证明 flag 生效、只是只值 10 MiB）。
- **死锁 vs 慢**：16 张卡全 100% util 两者都成立。**功耗才是判别量**——
  自旋 ~95 W @2700 MHz，真算 300–450 W。确认后再 py-spy 取栈（stage0 在
  `all_gather_object`、stage1 在 `barrier` = 集合不匹配）。
  已在门里加了"捕获即打印 traceback + 30 min PG 超时"，此后两次 OOM
  都是**秒级定位**而不是 20 分钟黑箱。

## 4. 一句话总结

语义链条完整且每一环都有真实权重的门背书。

**2026-07-22 更新**：attention TP4 分片（A 档）**已实现、已测、质量疑点已了结**
（−3 是抽样噪声），但 **§1.3 字面仍 FAIL，放行需人裁定是否重新标定门基准**。
它仍是三件事的共同前提（下列三条依然成立），只是瓶颈已从"能不能做"变成
"要不要放行"。**下一个交付层面的动作以 TARGET §10 Phase 1 为准（serving 骨架先行）**，
不是继续堆裸引擎优化。

⚠️ 下列第 2 条里"余 1.13 GiB 的字节账"**已被实测证伪**：分片后实测上限 **21 层
（PP2 需 22）**，仍缺 ~0.40 GiB/卡；对得上缺口的是 **FP8 常驻 attention 权重**
（估 +0.91，未实现、无质量门）。见 §3.6。

原文三条理由：

1. **单路速度**——E2F 实测当前形态（DP-attention 复制 attention 权重）的
   带宽天花板只有 76.2 tok/s，对外承诺的 ≥150 在这个形态下**不可达**；
2. **8 卡容量**——E3F 实测当前形态连权重都装不下（22 层 stage 载到第 19 层
   OOM），而"装得下"的那个字节账（余 1.13 GiB）**本身就是分片之后才有的**；
3. **标准版五行**——客户口径的五行不要求同时满足、每行可用各自配置，
   所以 8 卡是**五个独立的容量问题**（TARGET §7.7），而分片落地之前
   一行都无法有意义地重问。

⚠️ **注意 E3F 结论的范围**：证伪的是"8 卡跑吞吐档位"（M7 的 10–16K 聚合），
**不是"8 卡装不下模型"**。本仓库初稿曾写成后者——那会让 8 卡以"已关闭问题"
的身份进 TARGET §5、后续不再回头重问，而它实际上是个**还没被正确提问过的
问题**。已于 2026-07-21 收敛，见 E3F §5 的撤回说明。

M4 的 200–350 还需要 elementwise 尾巴折叠同时到位（E4F 已取下第一块 +2.31%，
逐位）；两处缺一不可，任一单独完成都停在 ~80 tok/s（E2F）。
